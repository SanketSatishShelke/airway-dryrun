.env

DATA_DIR=/mnt/data/airway-dryrun
REF_DIR=/mnt/data/airway-dryrun/reference
PROJECT_DIR=/home/priyasanket/projects/airway-dryrun

ANTHROPIC_API_KEY= (add later, when we build the LLM layer)
.Rproj.user
